<ul class="topic_path">
  <% items.each do |item| %>
    <li class="topic_path_item"><%= link_to "[X] #{item[:label]}",
                                            item[:path],
                                            :title => title.sub("%{label}", item[:label]) %></li>
  <% end %>
</ul>

